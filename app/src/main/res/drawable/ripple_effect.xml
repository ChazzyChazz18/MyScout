<!-- Here on color we assign the ripple color when touch -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:shape="rectangle"  android:color="@color/colorPrimary"
    tools:targetApi="lollipop">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="#000000" />
            <corners android:radius="15dp" />
        </shape>
    </item>
    <!-- Here we assign the default color to the button/view -->
    <item android:drawable="@drawable/button_border"/>
</ripple>